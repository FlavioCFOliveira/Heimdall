# DoT listener with TLS cert path that does not exist — triggers exit 2.
[roles]
authoritative = true

[[listeners]]
address  = "127.0.0.1"
port     = 8553
transport = "dot"
tls_cert = "/nonexistent/cert.pem"
tls_key  = "/nonexistent/key.pem"

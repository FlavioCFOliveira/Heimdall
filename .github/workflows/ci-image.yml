# Image smoke test — Sprint 48 task #482.
# Builds the container image for linux/amd64 and runs tests/image/smoke_ready.sh
# to verify that the container starts and becomes ready within 10 seconds.
#
# Implements the CI gate required by task #482 AC: "Smoke test green in CI.
# Failure to ready in 5s fails CI."

name: CI — image smoke

on:
  push:
    branches: [main]
    paths:
      - Dockerfile
      - "crates/**"
      - "contrib/heimdall.toml.example"
      - "tests/image/**"
      - ".github/workflows/ci-image.yml"
  pull_request:
    paths:
      - Dockerfile
      - "crates/**"
      - "contrib/heimdall.toml.example"
      - "tests/image/**"
      - ".github/workflows/ci-image.yml"

jobs:
  smoke:
    name: container image smoke test
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build image (linux/amd64)
        uses: docker/build-push-action@v6
        with:
          context: .
          file: Dockerfile
          platforms: linux/amd64
          push: false
          load: true
          tags: heimdall:test
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Run smoke test
        env:
          IMAGE: heimdall:test
          READY_TIMEOUT: "10"
        run: bash tests/image/smoke_ready.sh

      - name: Show container image size
        run: |
          SIZE=$(docker image inspect heimdall:test --format '{{.Size}}')
          SIZE_MB=$(echo "scale=1; $SIZE / 1048576" | bc)
          echo "Image size: ${SIZE_MB} MB"
          # AC: image must be < 50 MB
          python3 -c "
          import sys
          size = $SIZE
          limit = 50 * 1024 * 1024
          if size >= limit:
              print(f'FAIL: image size {size/1024/1024:.1f} MB exceeds 50 MB limit')
              sys.exit(1)
          else:
              print(f'PASS: image size {size/1024/1024:.1f} MB < 50 MB')
          "

#!/usr/bin/env bash
# SPDX-License-Identifier: MIT
# riscv64_bench.sh — RISC-V benchmark pipeline stub (PERF-029..032).
# Requires QEMU user-mode emulation or a native RISC-V host.
echo "RISC-V benchmark pipeline: cross-compilation and QEMU emulation wiring deferred."
echo "Target triple: riscv64gc-unknown-linux-gnu"
echo "Required: cross-compile with 'cargo build --target riscv64gc-unknown-linux-gnu'"
exit 0

# Invalid: contains an unknown top-level key.
# Used by check_config::unknown_key_exits_two (ROLE-021).
[roles]
authoritative = true

[[listeners]]
address = "127.0.0.1"
port = 5353
transport = "udp"

completely_unknown_top_level_key = "this must be rejected"

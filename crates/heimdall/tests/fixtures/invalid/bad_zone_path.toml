# Zone file path that does not exist — triggers exit 2.
[roles]
authoritative = true

[[listeners]]
address   = "127.0.0.1"
port      = 59154
transport = "udp"

[[zones.zone_files]]
origin = "example.com."
path = "/nonexistent/path/to/example.com.zone"
